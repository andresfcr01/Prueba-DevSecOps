apiVersion: v1
kind: Service
metadata:
  name: {{ include "prueba-devsecops.fullname" . }}
spec:
  selector:
    app: {{ include "prueba-devsecops.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5000
  type: {{ .Values.service.type }}
